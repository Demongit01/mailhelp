{
 "build": {
   "image": "fleek/next-js:node-16",
   "command": "npm install && npm run build && npm run export",
   "publicDir": "build",
   "baseDir": "frontend",
   "environment": {
     "SOME_ENV": "Build finished!"
   }
 }
}